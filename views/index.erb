<div class="jumbotron">
  <div class="container">
    <h2>QuickCuts</h2>
    <p>Succinct music reviews, period.</p>
    <p>If the review intrigues you, click on it to find out what's being reviewed. Discover new music through others' descriptions.</p>
  </div>
</div>
<div class="panel panel-default">
  <div class="panel-heading">Recent Posts</div>
  <div class="panel-body">
    <% @posts.each do |post| %>
      <div class="media">
        <a class="pull-left" href="#">
          <img class="media-object" src=<%= User.find(post.user_id).picture %>>
        </a>
        <div class="media-body">
          <h4 class="media-heading"><a href="/users/<%= post.user_id %>"><%= "#{User.find(post.user_id).full_name}" %></a></h4>
          <p><%= post.body %></p>
          <a href="/posts/<%= post.id %>">Link to this post</a>
        </div>
      </div>
    <% end %>
  </div>
</div>
