<% if flash[:notice] %>
  <%= flash[:notice] %>
<% end %>

<div class="jumbotron">
  <div class="container">
    <h2>QuickCuts</h2>
    <p>Succinct music reviews, period.</p>
  </div>
</div>
<div class="panel panel-default">
  <div class="panel-heading">Recent Posts</div>
  <div class="panel-body">
<% @posts.each do |post| %>
  <%= post.body %>
<% end %>
  </div>
</div>
